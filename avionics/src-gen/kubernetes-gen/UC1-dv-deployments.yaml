# generated by Accordant
apiVersion: apps/v1
kind: Deployment
metadata:
  name: SparkWorkerDep
spec:
  replicas: 3
  selector:
    matchLabels:
      component: SparkWorkerDep
  template:
    metadata:
      labels:
        component: SparkWorkerDep
    spec:
      containers: 
      - name: SparkWEnv
        image: ramhiser/spark:2.0.1
        command: [/spark-worker]
        ports:
        - containerPort: 8081
        resources:
          requests:
            cpu: 0.3
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: SparkMasterDep
spec:
  replicas: 1
  selector:
    matchLabels:
      component: SparkMasterDep
  template:
    metadata:
      labels:
        component: SparkMasterDep
    spec:
      containers: 
      - name: SparkMEnv
        image: ramhiser/spark:2.0.1
        command: [/spark-master]
        ports:
        - containerPort: 7077
        - containerPort: 8080
        resources:
          requests:
            cpu: 0.3
---


